{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "codegen": {
      "inputs": ["templates/**"]
    },
    "format": {
      "outputs": ["*.json", "*.tsx", "*.ts", "*.js", "*.md"]
    }
  }
}
